# 変更履歴

## 2.4.0 — 未リリース

### 追加
- アドレスバー。ドライブバーの右に今いるフォルダのパスを出す。クリック・`T`・`Ctrl`+`L` で編集し、`Enter` でジャンプ、`Ctrl`+`Enter` でエクスプローラーで開く、`Shift`+`Enter` でフォルダを参照。`↑` でフォルダ履歴、`↓` でクイックアクセス（補完の候補が出ている間は候補の選択）。フォルダ名の補完が効く。ファイルのパスを入れると、そのフォルダへ移ってカーソルを合わせる
- 「表示」メニューの「アドレスバー」で、ウィンドウごとに出し入れできる
- アドレスバーの先頭にフォルダのアイコンを置いた。クリックでエクスプローラーで開き、右クリックでフォルダのメニューを出す。ブックマークバーへドラッグすると登録でき、デスクトップへ落とすとショートカットができる
- ブックマークバー。フォルダ・ファイル・コマンド（外部ツールを含む）・グループを並べる。フォルダは押すと中身をメニューで開き（入れ子のフォルダはホバーで開く）、先頭の「このフォルダへジャンプ」で移る。ファイルは開き、コマンドは実行する
- ブックマークの右クリック。メニューを開いたまま右クリックでき、フォルダ・ファイルにはエクスプローラーのメニューも出る。編集・削除・クイックアクセスへの追加、現在のフォルダ・カーソル位置の項目・コマンド・グループの追加ができる
- ブックマークバーの上のドラッグ。ファイルリストやエクスプローラーから落として追加し、バーの項目を動かして並べ替える。落とす位置に線を出し、グループの上で少し止めると開く
- ブックマークの管理（「ブックマーク」メニュー）。ツリーで追加・編集・削除・並べ替え（ドラッグでグループの間も移せる）をし、バーの表示の形（アイコンと名前・アイコンだけ・名前だけ）を選ぶ
- 「ブックマーク」メニュー（`Alt`+`B`）。バーを隠していてもすべてのブックマークに届く。「表示」メニューの「ブックマークバー」でバーを出し入れできる
- 組み込みコマンドとグループにアイコンを付けた
- クイックアクセスにファイルとコマンドを登録できるようにした（設定画面の「ファイルを追加」「コマンドを追加」）。ファイルは開き、コマンドは実行する
- ブックマークやクイックアクセスのメニューをホイールで送れる

### 変更
- ドライブバーとアドレスバーを 1 行にまとめた。ウィンドウが狭いときは、ドライブのボタンを文字だけにする
- `Ctrl`+`L` の既定を「ダイレクトジャンプ」にした。すでに `Ctrl`+`L` に別の機能を割り当てていた場合は、その割り当てが残る
- ダイレクトジャンプのダイアログでも、ファイルのパスを受け付けるようにした
- メニューの項目の上下に余白を足し、押しやすくした
- フォルダを開くときに待つ上限を 5 秒から 15 秒にした（止まっていた HDD が回り始めるまでに「応答しません」が出ていた）
- ウィンドウが狭いとき、メニューバーとドライブバーに入りきらない項目を「»」から選べるようにした（ドライブバーはドライブを選ぶ枠を「»」の直下に出す）
- アドレスバーとダイレクトジャンプの `↓` の一覧には、クイックアクセスのうちフォルダだけを出す

### 修正
- 複数のウィンドウを開いているとき、あるウィンドウで足したクイックアクセスが、別のウィンドウの保存で消えることがあった

## 2.3.0 — 2026-09-18

### 追加
- 元に戻す（`Ctrl`+`Z`、「編集」メニュー）。名前の変更・移動・コピー・フォルダの作成・ショートカットの作成・ファイルの連結を、新しい順に 1 回ずつ戻せる。戻す前に内容を確認する。コピーや作成を戻すとごみ箱へ送る
- 記録した後に変更された項目や、元の場所に同じ名前の項目がある場合は戻さない。上書きしたコピーは戻せない

### 変更
- `Ctrl`+`Z` を固定のキーにした。キー割り当てで `Ctrl`+`Z` に別の機能を割り当てていた場合、その割り当ては使われなくなる

## 2.2.0 — 2026-09-17

### 追加
- インクリメンタルサーチ。`Ctrl`+`F` で、ステータスバーの上に入力欄を出す。前方一致でカーソルが動き、`↓` `↑` で次・前、`Enter` で確定、`Esc` で元の位置に戻る。マークは変わらない
- ファイルリストの空いた所を `Shift`+右クリックすると、エクスプローラーのフォルダ背景のメニュー（「新規作成」を含む）を出す。作成した項目にカーソルが合う
- ドライブバーを非表示にできるようにした（「表示」メニュー）。非表示のときは `L` でドライブを選ぶ小さな枠を出す
- ファイルリストからのドラッグに画像を付けた。ファイルリストとドライブバーに落とすとき、カーソルの横に「◯◯へ移動」「◯◯へコピー」と出る

### 変更
- 空いた所を右クリックしたときのメニューを、右クリックした位置に出すようにした（これまではカーソル行の直下）
- キー割り当ての設定の分類名「移動」を「ナビゲーション」にした
- 自分のフォルダへのドラッグを「落とせない」表示にした
- コピーしか許さないアプリからのドロップは、同じドライブでもコピーにするようにした

### 修正
- 高精度ホイールのマウスやタッチパッドで、ファイルリストの横スクロールが効かないことがあった

## 2.1.0 — 2026-09-16

### 追加
- マウスのボタン 3／4／5 にコマンドを割り当てられるようにした。4／5 の既定は「戻る」「進む」、3 (ホイールクリック)は未割り当て
- ボタンはウィンドウ内のどこで押しても効く。モーダルダイアログの表示中は効かない

## 2.0.2 — 2026-09-16

### 変更
- 初期登録の外部ツールに OS 標準のプログラムを設定した（エディタとビューアは `notepad.exe`、ターミナルは `cmd.exe`）。これまではパスが空で、`E` を押しても何も起きなかった

## 2.0.1 — 2026-09-16

### 修正
- クイックアクセスに同じフォルダを二重に登録できた問題
- 削除した外部ツールを指すキー割り当てが残る問題

### 開発
- 設定・ドメイン型・コマンド配線の構造を検証する仕組み（`schema/`）を追加した

## 2.0 — 2026-09-16

### 追加
- 外部ツールの一覧。件数を自由に増減できる
- コマンドマクロ（対象ファイルなどを引数に展開する）
- 外部ツールの設定ダイアログ
- 外部ツールキュー。順番に処理し、進行状況の表示と停止・強制終了ができる
- `Ctrl` 付きキーへの割り当て（48 枠）
- 登録した外部ツールをメニューバーと `G` のポップアップに表示

### 注意
- 1.2 の外部ツールの設定は読み込まない。更新後に設定し直す

## 1.2 — 2026-09-14

### 修正
- カーソル位置のファイルが消えたとき、カーソルが先頭へ飛ぶ問題
- フォルダ参照ツリーの初期表示がちらつく問題
- ファイルリストの `Shift+Enter` が関連付けで開いてしまう問題
- 高 DPI 環境でダイアログが拡大されない問題への対策

### 変更
- `V` の名称を「テキスト ビューア」に、「コマンドプロンプト」を「ターミナル」に変更
- デスクトップボタンをアイコン表示にし、ツールチップを付けた
- 動作環境の設定から説明文を外した

### 注意
- ターミナルの設定は保存先のキーが変わったため、更新後に設定し直す

## 1.1 — 2026-09-11

### 修正
- 末尾に全角空白を持つ名前を扱えない問題
- 日本語の並び順がエクスプローラーと異なる問題
- 2 つのウィンドウを開いた後、1 つに戻れない問題
- ファイルリストで IME がオンになるとコマンドが効かない問題
- インラインポップアップを開いた直後に先頭が選択されている問題
- DPI の変更に追従しない問題

### 変更
- 外部ツールとクイックアクセスの初期登録を空にした
- 列の先頭と末尾に余白を入れた
- バージョン情報をメニューに追加した

## 1.0 — 2026-09-11

最初のリリース。卓駆の基本操作（一覧・マーク・コピー／移動／削除／改名・ナビゲーション・外部ツール・各種設定・常駐）を備える。
