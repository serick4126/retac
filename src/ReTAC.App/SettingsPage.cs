namespace ReTAC.App;

/// <summary>
/// 統合した設定画面（<see cref="SettingsDialog"/>）の 6 ページ。並びはサイドバーの並び（R-102-1）と同じ。
/// </summary>
public enum SettingsPage
{
    Environment,
    ColorFont,
    KeyAssign,
    ExternalTool,
    DriveVisibility,
    QuickAccess,
}
